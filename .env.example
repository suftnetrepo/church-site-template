# Jerur backend (read-only data: settings, sliders, events, fellowship, regular services)
# JERUR_NEXT_API_KEY is THIS CHURCH's own key from Jerur — not a shared/master
# key. See lib/api-client.ts for how it's used.
JERUR_NEXT_BASE_URL=
JERUR_NEXT_API_KEY=

# Brevo (transactional email for contact / prayer request / testimony forms,
# and for any enabled feature-specific forms like WOFBI registration)
BREVO_API_KEY=
USER_NAME=
CHURCH_INBOX_EMAIL=
TEAM=
